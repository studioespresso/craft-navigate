{% import "_includes/forms" as forms %}

{{ forms.textField({
    id: 'name',
    name: 'name',
    label: "Name"|t,
    value: node.name,
}) }}

{% if node.type == 'url' %}
	{{ forms.textField({
        id: 'url',
        name: 'url',
        label: "URL"|t,
        value: node.url,
    }) }}
{% endif %}

{{ forms.lightswitchField({
    id:           'enabled',
    name:         'enabled',
    label:        'Enabled'|t,
    on:           node.enabled,
    instructions: 'Show or hide in navigation.'|t
}) }}

{{ forms.lightswitchField({
    id:           'blank',
    name:         'blank',
    label:        'Open in a new window'|t,
    on:           node.blank,
}) }}

{{ forms.textField({
    id: 'lasses',
    name: 'classes',
    label: "Classes"|t,
    instructions: "These css classes will be applied to this node's list item",
    value: node.classes,
}) }}

<style>
    .hud .elementeditor--navigate .main {
        padding: 0;
    }
</style>